name: 🚀 MASTER ORCHESTRATOR

on:
  workflow_dispatch:

jobs:
  # --- START IMMEDIATELY ---
  run-group-1:
    uses: ./.github/workflows/group1.yml
    secrets: inherit

  run-group-2:
    uses: ./.github/workflows/group2.yml
    secrets: inherit

  # --- START AFTER 5 MINS ---
  wait-5-mins:
    runs-on: ubuntu-latest
    steps:
      - name: ⏳ Sleeping
        run: sleep 300

  run-group-3:
    needs: wait-5-mins
    uses: ./.github/workflows/group3.yml
    secrets: inherit

  # --- START AFTER 10 MINS ---
  wait-10-mins:
    runs-on: ubuntu-latest
    steps:
      - name: ⏳ Sleeping
        run: sleep 600

  run-group-4:
    needs: wait-10-mins
    uses: ./.github/workflows/group4.yml
    secrets: inherit
